name: code

on:
  push:

jobs:
  code-checkout:
    runs-on: ubuntu-latest
    steps:
      - name: clone the code
        uses: actions/checkout@v7

      - name: print the code
        run: echo "Code Clone"
